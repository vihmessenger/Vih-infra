apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vih-messenger.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "vih-messenger.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "vih-messenger.name" . }}
    spec:
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
